aiogram==3.7.0
aiosqlite==0.20.0
aiohttp==3.9.5
python-dotenv==1.0.1
Pillow==10.3.0
numpy==1.26.4
googletrans==4.0.0rc1

# CLIP (AI поиск по фото)
# Устанавливается отдельно (см. README шаг 5):
# pip install git+https://github.com/openai/CLIP.git
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
